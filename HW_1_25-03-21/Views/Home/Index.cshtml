@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <div style="text-align:center;">
        @foreach (var item in ViewBag.Persons)
        {
            <h1>@item.FirstName + " " + @item.MiddleName</h1>
        }
    </div>
</div>
